from langchain_core.tools import tool


@tool
def calculate_expression(expression: str) -> str:
    """Вычисляет арифметическое выражение, переданное в виде строки.

    Используй для точных числовых расчётов: сложение, вычитание, умножение, деление,
    скобки, степени, проценты и другие операции, которые поддерживает движок вычислений
    (после реализации). Подходит для проверки формул, бюджетных сумм, долей, накоплений
    и любых вычислений, где важна арифметическая точность, а не оценка «на глаз».

    Не подставляй сюда задачи без явной математической формулы: смысловой разбор,
    инвестиционные советы и качественные выводы остаются за рассуждением агента.
    Параметр ``expression`` — одна строка с выражением (например, ``"(100 + 15) * 0.13"``).
    """
    pass


@tool
def web_search(query: str) -> str:
    """Ищет в интернете информацию по запросу и возвращает краткие релевантные сведения.

    Применяй **только** когда нужны **актуальные внешние факты**, которые нельзя надёжно
    восстановить из общих знаний: текущие цены и котировки, ключевая ставка и решения ЦБ,
    официальные курсы валют, недавние изменения норм, даты событий, свежие новости,
    влияющие на расчёт или проверку факта.

    **Не** используй поиск как универсальный ответ на любой вопрос пользователя:
    планирование, стратегии, разбор личной ситуации, цели и рекомендации формируй сам.
    Формулируй ``query`` конкретно (кто, что, за какой период), без лишней воды.
    """
    pass
