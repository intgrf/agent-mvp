---
name: example
description: Example composite skill — brief line shown in the system prompt.
---

# Example skill

This file demonstrates the layout: optional YAML frontmatter with `description`
for the short summary, then full Markdown instructions for the agent.

When performing tasks that match this skill, combine the tools listed in your
workflow steps here.
