---
name: financial-goal-trajectory-planning
description: Строит сценарии достижения финансовой цели, помогает выбрать конкретную траекторию и только после этого формирует `goal_steps` для сохранения цели. Использовать, когда нужно понять, как накопить нужную сумму к сроку, сравнить варианты достижения, выбрать сценарий и подготовить финальную цель к `save_goal` или `update_goal`.
---

## Назначение

Этот skill помогает превратить черновик цели в реалистичный план достижения: с понятной ежемесячной нагрузкой, несколькими сценариями, выбранной траекторией и финальными `goal_steps`.

## Когда применять

Применяй skill, если пользователь:
- уже знает сумму цели и хочет понять, как ее достичь;
- спрашивает "успею ли накопить?";
- хочет сравнить варианты "быстрее" против "комфортнее";
- допускает сценарий с кредитом или хочет понять, нужен ли он;
- просит построить финансовую траекторию к дате.

## Входные данные

Для качественного расчета собери:
- категорию цели;
- целевую сумму;
- текущие накопления;
- комфортный ежемесячный платеж;
- срок в месяцах или дату дедлайна;
- допустимый лимит кредита, если пользователь готов его рассматривать.

Если срок дан датой, переведи его в месяцы через `calculate_expression` только после того, как получишь или оценишь число месяцев по правилам проекта.

## Основной workflow

1. Уточни параметры цели или получи их из уже существующей цели через `get_goals`.
2. Проверь реалистичность исходных ожиданий:
   - хватает ли срока;
   - соответствует ли ежемесячный взнос доходам и расходам;
   - не занижена ли стартовая сумма.
3. Запусти `calculate_trajectories` с подтвержденными параметрами.
4. Сравни как минимум 2 сценария, если инструмент их возвращает:
   - без кредита;
   - с кредитом, если пользователь готов его рассматривать.
5. Объясни разницу человеческим языком:
   - скорость достижения;
   - ежемесячная нагрузка;
   - общая финансовая цена решения;
   - уровень риска и гибкости.
6. Если цель выглядит недостижимой, не маскируй это:
   - предложи увеличить срок;
   - уменьшить целевую сумму;
   - повысить ежемесячный взнос;
   - перенести часть цели в кредитный сценарий.
7. Попроси пользователя выбрать конкретный сценарий достижения.
8. Только после выбора сценария сформируй `goal_steps` как последовательность действий, вытекающих именно из выбранной траектории.
9. Сделай финальную сверку всех полей цели вместе с `goal_steps`.
10. Сохрани новую цель через `save_goal` или обнови существующую через `update_goal`.

## Обязательные правила

- Не обещай достижимость цели до расчета.
- Не продвигай кредитный сценарий как лучший по умолчанию.
- Если вариант с кредитом дороже и рискованнее, скажи это явно.
- Если данных не хватает, сначала задай уточняющие вопросы.
- Все числовые выводы, влияющие на совет, должны опираться на инструментальный расчет.
- Не формируй `goal_steps` до тех пор, пока пользователь явно не выбрал один сценарий.
- Не сохраняй цель без финальных `goal_steps`, привязанных к выбранной траектории.

## Как интерпретировать результаты

Оцени каждый сценарий по четырем критериям:

### 1. Достижимость

Хватает ли выбранного горизонта, чтобы закрыть цель без чрезмерной нагрузки.

### 2. Нагрузка на месяц

Насколько регулярный платеж выглядит комфортным. Если платеж вытесняет базовые траты или делает бюджет хрупким, помечай сценарий как напряженный.

### 3. Стоимость решения

Если есть кредит, явно выделяй, что пользователь выигрывает по времени, но может переплатить по стоимости и потерять гибкость.

### 4. Устойчивость

Остается ли у пользователя запас на непредвиденные расходы. Если его нет, напомни о риске срыва плана.

## Формат рекомендации

Ответ должен содержать:
- краткий вывод;
- 2-3 сценария;
- рекомендуемый вариант;
- приглашение выбрать конкретный сценарий.

Пример структуры:

```markdown
По этой цели вижу такие варианты:

1. Базовый сценарий: ...
2. Ускоренный сценарий: ...
3. Сценарий с кредитом: ...

Оптимальным выглядит вариант ..., потому что ...

Если согласен, зафиксирую именно этот сценарий и на его основе сформирую шаги цели.
```

## Как формировать `goal_steps`

Формируй `goal_steps` только после выбора сценария.

Шаги должны:
- быть привязаны к выбранной траектории;
- отражать способ достижения цели, а не абстрактные пожелания;
- быть короткими и исполнимыми;
- обычно укладываться в 3-7 пунктов.

Хорошая логика шагов:
1. зафиксировать стартовые условия;
2. закрепить регулярный взнос или схему финансирования;
3. описать действия по оптимизации бюджета или использованию кредита, если это часть сценария;
4. добавить контрольную точку пересмотра прогресса.

Примеры зависят от сценария:
- без кредита: "Настроить ежемесячное отложение 25000", "Сократить категорию необязательных трат на 10000";
- с кредитом: "Подготовить допустимый лимит кредита", "Накопить сумму первоначального взноса", "Проверить комфортный ежемесячный платеж".

## Сохранение или обновление цели

Если пользователь выбрал сценарий и согласовал финальные поля:
1. собери `goal_name`, `goal_category`, `goal_amount`, `plan_date`, `goal_steps`;
2. для новой цели вызови `save_goal`;
3. для существующей цели вызови `update_goal`;
4. коротко объясни, что шаги основаны на выбранной траектории достижения.

## Если пользователь хочет изменить цель

Если после расчета выяснилось, что цель в текущем виде неудобна или недостижима:
1. согласуй новую сумму, дату или параметры сценария;
2. заново сравни траектории при необходимости;
3. сформируй новые `goal_steps` только после повторного выбора сценария;
4. обнови цель через `update_goal`;
5. коротко объясни, что изменилось и почему.

## Дополнительные инструменты

- `calculate_expression`: для промежуточных вычислений, проверки взносов, долей и разницы между сценариями;
- `income_consult_tool`: если нужно уточнить факты о доходах перед выбором комфортного взноса;
- `outcome_consult_tool`: если нужно понять, какие траты уже конкурируют с планом;
- `web_search`: если пользователю нужен внешний контекст по рынку, ставкам или ориентиру стоимости.
